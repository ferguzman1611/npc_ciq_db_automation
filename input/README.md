# input/

Place the CIQ Excel file here before running the pipeline.

The filename must match the `INPUT_FILENAME` value defined in your `.env` file.

> This folder is git-ignored. Never commit input files to the repository.